#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs - function to check if number is absolute
 * Return: int
 * @n: int
 */

int _abs(int n)
{

    int r;

    r = abs(n);
    return (r);
}